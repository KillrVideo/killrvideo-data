# Astra DB Configuration
# Copy this file to .env.local and fill in your values

# Database name (as shown in Astra dashboard, required for --truncate option)
ASTRA_DB_NAME=your_database_name

# Keyspace name (must be created in Astra dashboard first)
ASTRA_KEYSPACE=killrvideo

# Path to your Secure Connect Bundle
# Download from: Astra Dashboard > Your Database > Connect > Drivers > Download Bundle
ASTRA_SCB_PATH=/path/to/your/secure-connect-bundle.zip

# Application Token credentials
# Create at: Astra Dashboard > Your Database > Connect > Create Token
# For DSBulk: CLIENT_ID must be literally "token", CLIENT_SECRET is your full AstraCS:... token
ASTRA_CLIENT_ID=token
ASTRA_CLIENT_SECRET=AstraCS:your_token_here

# Optional: Override output directory (defaults to ./output)
# OUTPUT_DIR=./output
